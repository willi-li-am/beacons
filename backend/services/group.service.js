const Group = require('../models/group.model')

const createGroup = (body) => {
    
    //add author to group automatically
    

}

const addToGroup = (users, invite) => {
    //set user to be an array
    //search which group by invite code
}